# """
# Django settings for project_name project.
# Generated by 'django-admin startproject' using Django 5.2.6.
# For more information on this file, see
# https://docs.djangoproject.com/en/5.2/topics/settings/
# For the full list of settings and their values, see
# https://docs.djangoproject.com/en/5.2/ref/settings/
# """
# from pathlib import Path
# import os  # Yeh line file ke top par add karein agar nahi hai
# import environ
# # Build paths inside the project like this: BASE_DIR / 'subdir'.
# BASE_DIR = Path(__file__).resolve().parent.parent
# # Quick-start development settings - unsuitable for production
# # See https://docs.djangoproject.com/en/5.2/howto/deployment/checklist/
# # SECURITY WARNING: keep the secret key used in production secret!
# SECRET_KEY = "django-insecure-rf^i_@h*%*-6bz7n5djy8d2r26z+e!y-s4=h2g83pv=uptw)gk"
# # SECURITY WARNING: don't run with debug turned on in production!
# DEBUG = True
# ALLOWED_HOSTS = []
# # Application definition
# INSTALLED_APPS = [
#     "django.contrib.admin",
#     "django.contrib.auth",
#     "django.contrib.contenttypes",
#     "django.contrib.sessions",
#     "django.contrib.messages",
#     "django.contrib.staticfiles",
#     'django.contrib.humanize',
#     "app",
#     "user_tests",
#     "widget_tweaks",
# ]
# MIDDLEWARE = [
#     "django.middleware.security.SecurityMiddleware",
#     "django.contrib.sessions.middleware.SessionMiddleware",
#     "django.middleware.common.CommonMiddleware",
#     "django.middleware.csrf.CsrfViewMiddleware",
#     "django.contrib.auth.middleware.AuthenticationMiddleware",
#     "django.contrib.messages.middleware.MessageMiddleware",
#     "django.middleware.clickjacking.XFrameOptionsMiddleware",
# ]
# ROOT_URLCONF = "ai_test.urls"
# TEMPLATES = [
#     {
#         "BACKEND": "django.template.backends.django.DjangoTemplates",
#         "DIRS": [os.path.join(BASE_DIR, "templates")],  # Yeh line add karein
#         "APP_DIRS": True,
#         "OPTIONS": {
#             "context_processors": [
#                 "django.template.context_processors.debug",
#                 "django.template.context_processors.request",
#                 "django.contrib.auth.context_processors.auth",
#                 "django.contrib.messages.context_processors.messages",
#             ],
#         },
#     },
# ]
# WSGI_APPLICATION = "ai_test.wsgi.application"
# # --- ADD YOUR EMAIL SETTING HERE ---
# EMAIL_BACKEND = "django.core.mail.backends.smtp.EmailBackend"
# EMAIL_HOST = "smtp.gmail.com"
# EMAIL_PORT = 587
# EMAIL_USE_TLS = True
# EMAIL_HOST_USER = "shweta.ladne.averybit@gmail.com"
# EMAIL_HOST_PASSWORD = "qwgh jagp euzh qcwi"
# DEFAULT_FROM_EMAIL = "shweta.ladne.averybit@gmail.com" # Yah bhi add kar dein
# DATABASES = {
#     "default": {
#         "ENGINE": "django.db.backends.postgresql",
#         "NAME": "aiTest",
#         "USER": "postgres",
#         "PASSWORD": "12345",
#         "HOST": "localhost",
#         "PORT": "5432",
#     }
# }
# # DATABASES = {
# #     "default": {
# #         "ENGINE": "django.db.backends.postgresql",
# #         "NAME": "written_test_ydbx",
# #         "USER": "written_test_ydbx_user",
# #         "PASSWORD": "9DDegu2JdEFSW9Qw5MiTcLCZT1Mqh690",
# #         "HOST": "dpg-d4arav9e2q1c73b72klg-a.oregon-postgres.render.com",
# #         "PORT": "5432",
# #         "OPTIONS": {
# #             "sslmode": "require",
# #         },
# #     }
# # }
# ALLOWED_HOSTS = ["127.0.0.1", "localhost", "192.168.1.10", "10.0.0.42"]
# # Password validation
# # https://docs.djangoproject.com/en/5.2/ref/settings/#auth-password-validators
# AUTH_PASSWORD_VALIDATORS = [
#     {
#         "NAME": "django.contrib.auth.password_validation.UserAttributeSimilarityValidator",
#     },
#     {
#         "NAME": "django.contrib.auth.password_validation.MinimumLengthValidator",
#     },
#     {
#         "NAME": "django.contrib.auth.password_validation.CommonPasswordValidator",
#     },
#     {
#         "NAME": "django.contrib.auth.password_validation.NumericPasswordValidator",
#     },
# ]
# env = environ.Env(
#     # Set casting default for API key (e.g., read as a string)
#     GEMINI_API_KEY=(str, ""),
#     OPENAI_API_KEY=(str, "")
# )
# environ.Env.read_env(os.path.join(BASE_DIR, ".env"))
# # settings.py
# GEMINI_API_KEY = env("GEMINI_API_KEY")
# OPENAI_API_KEY = env("OPENAI_API_KEY")
# print(f"DEBUG: OpenAI Key Loaded: {'Yes' if OPENAI_API_KEY else 'No'}") # DO NOT PRINT THE ACTUAL KEY FOR SECURITY
# # Internationalization
# # https://docs.djangoproject.com/en/5.2/topics/i18n/
# LANGUAGE_CODE = "en-us"
# TIME_ZONE = "UTC"
# USE_I18N = True
# USE_TZ = True
# # Static files (CSS, JavaScript, Images)
# # https://docs.djangoproject.com/en/5.2/howto/static-files/
# STATIC_URL = "static/"
# # Default primary key field type
# # https://docs.djangoproject.com/en/5.2/ref/settings/#default-auto-field
# DEFAULT_AUTO_FIELD = "django.db.models.BigAutoField"
# # Django ko batayein ki global static folder kahan hai
# STATICFILES_DIRS = [
#     os.path.join(BASE_DIR, "static"),
# ]


# settings.py for Render Deployment

from pathlib import Path
import os 
import environ
import dj_database_url  # New import: Database URL parsing ke liye

# Build paths inside the project like this: BASE_DIR / 'subdir'.
BASE_DIR = Path(__file__).resolve().parent.parent

# Initialize environment variables
env = environ.Env(
    # Production settings ke liye DEBUG default False rakhein
    DEBUG=(bool, False), 
    GEMINI_API_KEY=(str, ""),
    OPENAI_API_KEY=(str, ""),
    # DATABASE_URL ke liye default empty string rakhein agar file mein define nahi hai
    DATABASE_URL=(str, "") 
)
environ.Env.read_env(os.path.join(BASE_DIR, ".env"))

# Quick-start development settings - unsuitable for production
# SECURITY WARNING: keep the secret key used in production secret!
# 1. SECRET_KEY ko Environment Variable se load karein
SECRET_KEY = env('SECRET_KEY', default='django-insecure-rf^i_@h*%*-6bz7n5djy8d2r26z+e!y-s4=h2g83pv=uptw)gk')

# SECURITY WARNING: don't run with debug turned on in production!
# 2. DEBUG ko Environment Variable se load karein (Render par False hona chahiye)
DEBUG = env('DEBUG')

# 3. ALLOWED_HOSTS ko Environment Variable se ya Render ke URL se load karein
# '*' Render par default ke liye theek hai ya fir env.list('ALLOWED_HOSTS') use karein.
ALLOWED_HOSTS = env.list('ALLOWED_HOSTS', default=['ai-written-test-5tny.onrender.com'])
if DEBUG:
    ALLOWED_HOSTS += ["127.0.0.1", "localhost"]

# Application definition
INSTALLED_APPS = [
    "django.contrib.admin",
    "django.contrib.auth",
    "django.contrib.contenttypes",
    "django.contrib.sessions",
    "django.contrib.messages",
    "django.contrib.staticfiles",
    'django.contrib.humanize',
    # WhiteNoise ke liye koi app install karne ki zarurat nahi hai, bas middleware mein add karein
    "app",
    "user_tests",
    "widget_tweaks",
]

MIDDLEWARE = [
    # 4. WhiteNoise middleware ko Security aur Session middleware ke beech add karein
    "django.middleware.security.SecurityMiddleware",
    "whitenoise.middleware.WhiteNoiseMiddleware",
    "django.contrib.sessions.middleware.SessionMiddleware",
    "django.middleware.common.CommonMiddleware",
    "django.middleware.csrf.CsrfViewMiddleware",
    "django.contrib.auth.middleware.AuthenticationMiddleware",
    "django.contrib.messages.middleware.MessageMiddleware",
    "django.middleware.clickjacking.XFrameOptionsMiddleware",
]

ROOT_URLCONF = "ai_test.urls"

TEMPLATES = [
    {
        "BACKEND": "django.template.backends.django.DjangoTemplates",
        "DIRS": [os.path.join(BASE_DIR, "templates")],
        "APP_DIRS": True,
        "OPTIONS": {
            "context_processors": [
                "django.template.context_processors.debug",
                "django.template.context_processors.request",
                "django.contrib.auth.context_processors.auth",
                "django.contrib.messages.context_processors.messages",
            ],
        },
    },
]

WSGI_APPLICATION = "ai_test.wsgi.application"

# --- EMAIL SETTING ---
EMAIL_BACKEND = "django.core.mail.backends.smtp.EmailBackend"
EMAIL_HOST = "smtp.gmail.com"
EMAIL_PORT = 587
EMAIL_USE_TLS = True
# SECURITY WARNING: Email credentials ko .env se load karein!
EMAIL_HOST_USER = env("EMAIL_HOST_USER", default="shweta.ladne.averybit@gmail.com") 
EMAIL_HOST_PASSWORD = env("EMAIL_HOST_PASSWORD", default="qwgh jagp euzh qcwi")
DEFAULT_FROM_EMAIL = "shweta.ladne.averybit@gmail.com" # Yah bhi add kar dein

# 5. DATABASES: Local setting ko comment karein aur Production setting ko activate karein.
# Render automatically DATABASE_URL environment variable set karta hai.

# Local Database (Commented out)
# DATABASES = {
#     "default": {
#         "ENGINE": "django.db.backends.postgresql",
#         "NAME": "aiTest",
#         "USER": "postgres",
#         "PASSWORD": "12345",
#         "HOST": "localhost",
#         "PORT": "5432",
#     }
# }

# Production Database (Render-friendly)
DATABASES = {
    'default': dj_database_url.config(
        default=env('DATABASE_URL'), # Render se DATABASE_URL load karega
        conn_max_age=600,
        ssl_require=True # SSL mode Render ki PostgreSQL ke liye required hai
    )
}

# AI Keys
GEMINI_API_KEY = env("GEMINI_API_KEY")
OPENAI_API_KEY = env("OPENAI_API_KEY")
print(f"DEBUG: OpenAI Key Loaded: {'Yes' if OPENAI_API_KEY else 'No'}") 

# Internationalization...
LANGUAGE_CODE = "en-us"
TIME_ZONE = "UTC"
USE_I18N = True
USE_TZ = True

# 6. STATIC FILE SETTINGS: WhiteNoise ke liye Static files ki configuration.


STATIC_URL = "static/"


STATIC_ROOT = BASE_DIR / 'staticfiles' 
STATICFILES_DIRS = [
    BASE_DIR / "static",
]

# 7. WhiteNoise ke liye Storage Backend
STORAGES = {
    "staticfiles": {
        "BACKEND": "whitenoise.storage.CompressedManifestStaticFilesStorage",
    },
}

# Default primary key field type
DEFAULT_AUTO_FIELD = "django.db.models.BigAutoField"